# SMTP — required for the contact form (/api/contact)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username@example.com
SMTP_PASS=your-smtp-password

# Google Reviews — optional; falls back to static reviews if not set
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
